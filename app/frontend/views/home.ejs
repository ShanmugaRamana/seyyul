<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Seyyul</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/home.css">
</head>

<body>
    <div class="home-container">
        <header class="header-bar">
            <div class="brand">Seyyul</div>
            <div class="user-controls">
                <span id="headerUserName" style="margin-right: 15px;"></span>
                <button class="btn btn-secondary btn-sm" onclick="logout()">Log Out</button>
            </div>
        </header>

        <main class="welcome-section">
            <div class="user-avatar">👋</div>
            <h1 id="welcomeMessage">Welcome back!</h1>
            <p>You are successfully logged in.</p>

            <div class="dashboard-grid">
                <div class="card">
                    <h3>My Modules</h3>
                    <p style="margin-top: 1rem; color: #888;">No modules yet. Start learning!</p>
                </div>
                <div class="card">
                    <h3>Progress</h3>
                    <p style="margin-top: 1rem; color: #888;">Track your learning journey here.</p>
                </div>
            </div>
        </main>
    </div>

    <script>
        // Auth Check
        const userStr = localStorage.getItem('user');
        if (!userStr) {
            window.location.href = '/login';
        } else {
            const user = JSON.parse(userStr);
            document.getElementById('headerUserName').textContent = user.name;
            document.getElementById('welcomeMessage').textContent = `Welcome back, ${user.name}!`;
        }

        function logout() {
            localStorage.removeItem('token');
            localStorage.removeItem('user');
            window.location.href = '/login';
        }
    </script>
</body>

</html>